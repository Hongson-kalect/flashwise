import { TextStyle, View, ViewStyle } from "react-native";

type Props = {
  question: any;
  wrapperStyle?: ViewStyle;
  textStyle?: TextStyle;
};
const CardKeyword = () => <View></View>;
export default CardKeyword;
