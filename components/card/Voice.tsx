import { View } from "react-native";
import AppText from "../AppText";

const CardVoice = ({ children }: { children: string }) => (
  <View>
    <AppText>Cạt voi</AppText>
  </View>
);

export default CardVoice;
