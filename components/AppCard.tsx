// Đoạn này liên quan đến dữ liệu roài

type AppCardProps = {
  image?: string;
  word: string;
  definition: string;
  example?: string;
  onPress: () => void;
  fromLanguage?: string;
  toLanguage?: string;
};

const AppCard = {};

export default AppCard;
