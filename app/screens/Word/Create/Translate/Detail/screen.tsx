import AppText from "@/components/AppText";
import { View } from "react-native";

type Props = {};

export default function TranslateDetail({}: Props) {
  return (
    <View>
      <AppText>Traslate detail</AppText>
    </View>
  );
}
